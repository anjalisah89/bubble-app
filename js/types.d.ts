// Just simple types for the project, nothing to do with the working of the project

export interface CanvasProps {
    innerWidth: number;
    innerHeight: number;
    ctx: CanvasRenderingContext2D;
    canvas: HTMLCanvasElement;
}